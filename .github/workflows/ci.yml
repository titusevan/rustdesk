name: CI

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-2022

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install NASM
        uses: ilammy/setup-nasm@v1

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: vcpkg setup
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: '120deac3062162151622ca4860575a33844ba10b'

      # Добавляем явную установку зависимостей, которые просит компилятор
      - name: Install vcpkg dependencies
        shell: bash
        run: |
          $VCPKG_ROOT/vcpkg install libvpx libyuv opus --triplet x64-windows-static

      - name: Cargo Build
        shell: bash
        env:
          RS_ID_SERVER: ${{ secrets.RS_ID_SERVER }}
          RS_PUB_KEY: ${{ secrets.RS_PUB_KEY }}
          VCPKG_ROOT: ${{ github.workspace }}/vcpkg
        run: |
          # Указываем Rust использовать статическую сборку зависимостей
          export VCPKGRS_DYNAMIC=0
          cargo build --release --target ${{ matrix.target }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: RustDesk-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/*.exe
